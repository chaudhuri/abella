{
  "name": "examples",
  "version": "2.0.8-dev",
  "description": "",
  "main": "./js/abella_doc.js",
  "scripts": {
    "build": "tsc && esbuild ./js/abella_doc.js --color=false --bundle --minify --format=esm --outfile=dist/abella_doc.js",
    "css": "npx tailwindcss --output css/style.css --input css/style-src.css --minify",
    "clean": "rimraf build dist"
  },
  "keywords": [],
  "author": "Kaustuv Chaudhuri <kaustuv@chaudhuri.info>",
  "license": "GPL-3",
  "devDependencies": {
    "@types/node": "^20.8.4",
    "esbuild": "^0.19.4",
    "rimraf": "^5.0.5",
    "tailwindcss": "^3.3.3",
    "typescript": "^5.2.2"
  }
}
